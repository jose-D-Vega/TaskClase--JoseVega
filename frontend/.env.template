# URL base de la API. 
# Vite requiere que las variables empiecen con VITE_ para que sean accesibles en el frontend.
# Para acceso local: http://localhost:4000/api
# Para acceso desde la red (ej. 172.26.4.3): http://172.26.4.3:4000/api
VITE_API_URL=http://localhost:4000/api
